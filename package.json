{
  "name": "www.kamesuta.com-monorepo",
  "version": "0.0.0",
  "repository": "https://github.com/KamePowerWorld/www.kamesuta.com",
  "private": true,
  "engines": {
    "node": ">=18",
    "pnpm": ">=7"
  },
  "packageManager": "pnpm@7.17.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "fix": "turbo run fix",
    "install": "turbo run install",
    "minify": "turbo run minify"
  },
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
